# Q17 – Add Label to Deployment Pod Template
A Deployment is running but its pod template is missing a required label. You cannot use `kubectl edit` directly — learn the correct technique.
Wait for setup, then click **Next**.
