# Q1 – Create Secret from Hardcoded Variables
A Deployment has three hardcoded database credentials in its env vars. Find the values, move them into a Secret, and update the Deployment to reference it.
Wait for setup, then click **Next**.
